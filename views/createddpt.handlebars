{{!-- {{#each departments as | department |}} --}}
{{log department}}
<div class="col-auto">
  <h2>{{department.name}}, Department ID: {{department.id}}</h2>
</div>

<div class="row mt-4">
  <div class="col-md-6">
    <h3>Meet the Team:</h3>
    <div class="col-md-6 project-list">
      <h3>Department Manager</h3>
      <div class="row mb-2">
        <div class="col-md-8">
          <h4>{{department.manager}}</h4>
        </div>
      </div>
    </div>
  {{!-- {{/each}} --}}

    {{#each employees as | employee |}}
    {{log employee}}
    <div class="col-md-6 project-list">
      <div class="row mb-2">
        <div class="col-md-8">
          <h4>{{employee.name}}</a></h4>
          <p>{{employee.role}}</p>
          <p>{{employee.description}}</p>
        </div>
        <div class="col-md-4">
          {{!-- <button class="btn btn-sm btn-danger" data-id="{{project-id}}">DELETE</button> --}}
        </div>
      </div>
    </div>
    {{/each}}


    <div class="col">
      <a href="/newemployee"><button id="submitbtn">Add new employee</button></a>
    </div>
  </div>
  </div>